DROP TABLE IF EXISTS TB_FW_AUDIT_LOG;
DROP TABLE IF EXISTS TB_FW_TX_LOG;
DROP TABLE IF EXISTS TB_FW_TX_STATUS;
DROP TABLE IF EXISTS TB_TX_TRANSACTION_LOG;
DROP TABLE IF EXISTS TB_MSG_FILE;
DROP TABLE IF EXISTS TB_MSG_MESSAGE;
DROP TABLE IF EXISTS TB_MSG_AUDIT_LOG;
DROP TABLE IF EXISTS TB_XPILOT_SESSION;
DROP TABLE IF EXISTS TB_CAPACITY_ACCOUNT;
DROP TABLE IF EXISTS TB_JUNMUN_DEFINITION;
DROP TABLE IF EXISTS TB_ST_USER_PROFILE;

CREATE TABLE TB_MSG_MESSAGE (
    MESSAGE_ID        BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    MESSAGE_CODE      VARCHAR(50)  NOT NULL UNIQUE,
    MESSAGE_NAME      VARCHAR(100) NOT NULL,
    MESSAGE_TYPE      VARCHAR(20)  NOT NULL,
    CHANNEL_CODE      VARCHAR(30)  NOT NULL,
    LOCALE            VARCHAR(10)  NOT NULL,
    MESSAGE_CONTENT   VARCHAR(4000) NOT NULL,
    DISPLAY_START_AT  TIMESTAMP,
    DISPLAY_END_AT    TIMESTAMP,
    USE_YN            CHAR(1) DEFAULT 'Y' NOT NULL,
    CREATED_BY        VARCHAR(50) NOT NULL,
    CREATED_AT        TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_BY        VARCHAR(50) NOT NULL,
    UPDATED_AT        TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE INDEX IX_MSG_MESSAGE_01 ON TB_MSG_MESSAGE (MESSAGE_TYPE, CHANNEL_CODE, USE_YN);
CREATE INDEX IX_MSG_MESSAGE_02 ON TB_MSG_MESSAGE (DISPLAY_START_AT, DISPLAY_END_AT);

CREATE TABLE TB_MSG_FILE (
    FILE_ID           BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ORIGINAL_NAME     VARCHAR(255) NOT NULL,
    STORED_NAME       VARCHAR(255) NOT NULL,
    CONTENT_TYPE      VARCHAR(120) NOT NULL,
    FILE_SIZE         BIGINT NOT NULL,
    STORAGE_PATH      VARCHAR(500) NOT NULL,
    BIZ_CATEGORY      VARCHAR(30) NOT NULL,
    DESCRIPTION       VARCHAR(500),
    USE_YN            CHAR(1) DEFAULT 'Y' NOT NULL,
    CREATED_BY        VARCHAR(50) NOT NULL,
    CREATED_AT        TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_BY        VARCHAR(50) NOT NULL,
    UPDATED_AT        TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE INDEX IX_MSG_FILE_01 ON TB_MSG_FILE (BIZ_CATEGORY, USE_YN);
CREATE INDEX IX_MSG_FILE_02 ON TB_MSG_FILE (ORIGINAL_NAME);

CREATE TABLE TB_TX_TRANSACTION_LOG (
    TX_LOG_ID              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    REQUEST_URI            VARCHAR(200) NOT NULL,
    HTTP_METHOD            VARCHAR(10) NOT NULL,
    GUID                   VARCHAR(80) NOT NULL,
    TRACE_ID               VARCHAR(80) NOT NULL,
    SPAN_ID                VARCHAR(50),
    TRANSACTION_ID         VARCHAR(50) NOT NULL,
    INTERFACE_ID           VARCHAR(50),
    SERVICE_ID             VARCHAR(50) NOT NULL,
    REQUEST_DATE_TIME      VARCHAR(40),
    RESPONSE_DATE_TIME     VARCHAR(40),
    SOURCE_SYSTEM_ID       VARCHAR(50),
    TARGET_SYSTEM_ID       VARCHAR(50),
    CHANNEL_ID             VARCHAR(50),
    TERMINAL_ID            VARCHAR(50),
    USER_ID                VARCHAR(50),
    BRANCH_ID              VARCHAR(20),
    CENTER_ID              VARCHAR(50),
    AP_ID                  VARCHAR(50),
    REQUEST_TYPE           VARCHAR(20),
    MESSAGE_TYPE           VARCHAR(20),
    VERSION                VARCHAR(10),
    CLIENT_IP              VARCHAR(50),
    CTRL_TIMEOUT           INT,
    CTRL_RETRY_YN          CHAR(1),
    CTRL_RETRY_COUNT       INT,
    CTRL_PAGE_NO           INT,
    CTRL_PAGE_SIZE         INT,
    CTRL_TOTAL_COUNT       BIGINT,
    SEC_MASKING_LEVEL      VARCHAR(20),
    SEC_DATA_GRADE         VARCHAR(20),
    SEC_ACCESS_PURPOSE     VARCHAR(50),
    SEC_AUDIT_REQUIRED_YN  CHAR(1),
    ERR_RESULT_CODE        VARCHAR(20) NOT NULL,
    ERR_RESULT_MESSAGE     VARCHAR(200),
    ERR_ERROR_CODE         VARCHAR(50),
    ERR_ERROR_MESSAGE      VARCHAR(200),
    ERR_ERROR_DETAIL       VARCHAR(500),
    ERR_ERROR_SYSTEM_ID    VARCHAR(50),
    ERR_ERROR_DATE_TIME    VARCHAR(40),
    CREATED_AT             TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE INDEX IX_TX_LOG_01 ON TB_TX_TRANSACTION_LOG (GUID, TRACE_ID);
CREATE INDEX IX_TX_LOG_02 ON TB_TX_TRANSACTION_LOG (TRANSACTION_ID, SERVICE_ID);
CREATE INDEX IX_TX_LOG_03 ON TB_TX_TRANSACTION_LOG (ERR_RESULT_CODE, USER_ID);
CREATE INDEX IX_TX_LOG_04 ON TB_TX_TRANSACTION_LOG (CREATED_AT);

CREATE TABLE TB_MSG_AUDIT_LOG (
    AUDIT_ID          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    GUID              VARCHAR(80) NOT NULL,
    TRACE_ID          VARCHAR(80) NOT NULL,
    USER_ID           VARCHAR(50) NOT NULL,
    ACTION_TYPE       VARCHAR(30) NOT NULL,
    TARGET_ID         VARCHAR(80),
    RESULT_CODE       VARCHAR(20) NOT NULL,
    CREATED_AT        TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE TABLE TB_CAPACITY_ACCOUNT (
    ACCOUNT_NUMBER         VARCHAR(30)  NOT NULL PRIMARY KEY,
    NAME                   VARCHAR(100),
    IDENTIFICATION_NUMBER  VARCHAR(20),
    INTEREST_RATE          DOUBLE,
    LAST_TRANSACTION       TIMESTAMP,
    PASSWORD               VARCHAR(100),
    NET_AMOUNT             DOUBLE,
    ACCOUNT_TYPE           VARCHAR(20),
    STATUS                 VARCHAR(20),
    CURRENCY               VARCHAR(10),
    CREATED_DATE           TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UPDATED_DATE           TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IX_CAPACITY_ACCOUNT_01 ON TB_CAPACITY_ACCOUNT (ACCOUNT_TYPE, STATUS);

CREATE TABLE TB_XPILOT_SESSION (
    PILOT_ID           VARCHAR(40)  NOT NULL PRIMARY KEY,
    PILOT_NAME         VARCHAR(200) NOT NULL,
    TARGET_MODULE      VARCHAR(50)  NOT NULL,
    SOURCE_STRUCTURE   VARCHAR(200),
    TARGET_STRUCTURE   VARCHAR(200),
    STATUS             VARCHAR(20)  NOT NULL,
    ENV_RUN_ID         VARCHAR(80),
    NOTE               VARCHAR(500),
    CREATED_DATE       TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UPDATED_DATE       TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IX_XPILOT_SESSION_01 ON TB_XPILOT_SESSION (TARGET_MODULE, STATUS);

CREATE TABLE TB_ST_USER_PROFILE (
    USER_ID               VARCHAR(30) NOT NULL PRIMARY KEY,
    USER_NAME             VARCHAR(100),
    EMAIL                 VARCHAR(200),
    PHONE_NUMBER          VARCHAR(30),
    ROLE_CODE             VARCHAR(20),
    STATUS                VARCHAR(20),
    CREATED_DATE          TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UPDATED_DATE          TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IX_ST_USER_PROFILE_01 ON TB_ST_USER_PROFILE (ROLE_CODE, STATUS);

CREATE TABLE TB_JUNMUN_DEFINITION (
    DEFINITION_ID     BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    MESSAGE_CODE      VARCHAR(50)  NOT NULL UNIQUE,
    MESSAGE_NAME      VARCHAR(200) NOT NULL,
    TRANSACTION_ID    VARCHAR(50)  NOT NULL,
    SERVICE_ID        VARCHAR(50)  NOT NULL,
    DIRECTION         VARCHAR(10)  NOT NULL,
    STANDARD_VERSION  VARCHAR(30)  NOT NULL,
    DOCUMENT_REF      VARCHAR(300),
    LAYOUT_JSON       CLOB         NOT NULL,
    SAMPLE_JSON       CLOB,
    DESCRIPTION       VARCHAR(500),
    USE_YN            CHAR(1) DEFAULT 'Y' NOT NULL,
    CREATED_BY        VARCHAR(50) NOT NULL,
    CREATED_AT        TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_BY        VARCHAR(50) NOT NULL,
    UPDATED_AT        TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE INDEX IX_JUNMUN_DEF_01 ON TB_JUNMUN_DEFINITION (DIRECTION, USE_YN);
CREATE INDEX IX_JUNMUN_DEF_02 ON TB_JUNMUN_DEFINITION (TRANSACTION_ID, SERVICE_ID);

-- 전처리·후처리 모듈 (설계서 11.1~11.3)
CREATE TABLE TB_FW_TX_STATUS (
    TX_STATUS_ID      BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    GUID              VARCHAR(80)  NOT NULL,
    TRACE_ID          VARCHAR(80)  NOT NULL,
    TRANSACTION_ID    VARCHAR(50)  NOT NULL,
    SERVICE_ID        VARCHAR(50)  NOT NULL,
    USER_ID           VARCHAR(50),
    BRANCH_ID         VARCHAR(20),
    CHANNEL_ID        VARCHAR(50),
    STATUS            VARCHAR(20)  NOT NULL,
    RESULT_CODE       VARCHAR(20),
    ERROR_CODE        VARCHAR(50),
    REQUEST_TIME      TIMESTAMP,
    START_TIME        TIMESTAMP,
    END_TIME          TIMESTAMP,
    ELAPSED_TIME      BIGINT,
    IDEMPOTENCY_KEY   VARCHAR(80),
    RETRY_YN          CHAR(1) DEFAULT 'N',
    RETRY_COUNT       INT DEFAULT 0,
    CREATED_AT        TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDATED_AT        TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);
CREATE INDEX IX_FW_TX_STATUS_01 ON TB_FW_TX_STATUS (GUID);
CREATE INDEX IX_FW_TX_STATUS_02 ON TB_FW_TX_STATUS (TRANSACTION_ID, SERVICE_ID);
CREATE INDEX IX_FW_TX_STATUS_03 ON TB_FW_TX_STATUS (IDEMPOTENCY_KEY);

CREATE TABLE TB_FW_TX_LOG (
    LOG_ID            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    GUID              VARCHAR(80)  NOT NULL,
    TRACE_ID          VARCHAR(80)  NOT NULL,
    SERVICE_ID        VARCHAR(50)  NOT NULL,
    USER_ID           VARCHAR(50),
    REQUEST_URI       VARCHAR(200),
    HTTP_METHOD       VARCHAR(10),
    RESULT_CODE       VARCHAR(20)  NOT NULL,
    ERROR_CODE        VARCHAR(50),
    AP_ID             VARCHAR(50),
    DB_TIME           BIGINT,
    EXT_TIME          BIGINT,
    TOTAL_TIME        BIGINT,
    LOG_TIME          TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);
CREATE INDEX IX_FW_TX_LOG_01 ON TB_FW_TX_LOG (GUID, TRACE_ID);
CREATE INDEX IX_FW_TX_LOG_02 ON TB_FW_TX_LOG (SERVICE_ID, RESULT_CODE);
CREATE INDEX IX_FW_TX_LOG_03 ON TB_FW_TX_LOG (LOG_TIME);

CREATE TABLE TB_FW_AUDIT_LOG (
    AUDIT_ID          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    GUID              VARCHAR(80)  NOT NULL,
    USER_ID           VARCHAR(50)  NOT NULL,
    BRANCH_ID         VARCHAR(20),
    MENU_ID           VARCHAR(50),
    FUNCTION_ID       VARCHAR(50),
    CUSTOMER_ID       VARCHAR(30),
    ACTION_TYPE       VARCHAR(30)  NOT NULL,
    ACCESS_PURPOSE    VARCHAR(100),
    MASKING_LEVEL     VARCHAR(20),
    RESULT_CODE       VARCHAR(20)  NOT NULL,
    CLIENT_IP         VARCHAR(50),
    AUDIT_TIME        TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);
CREATE INDEX IX_FW_AUDIT_01 ON TB_FW_AUDIT_LOG (USER_ID, ACTION_TYPE);
CREATE INDEX IX_FW_AUDIT_02 ON TB_FW_AUDIT_LOG (GUID);
CREATE INDEX IX_FW_AUDIT_03 ON TB_FW_AUDIT_LOG (AUDIT_TIME);
